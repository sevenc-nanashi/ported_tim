---
globs: ["**/*.lua"]
description: "Luaスクリプトに関するルール。"
---

# Luaスクリプトに関するルール

## グローバル変数

基本的にスクリプト間で共有する必要のない変数は、グローバル変数を使用しないようにしてください。
グローバル変数を使用する場合は、`T_`というプレフィックスを付けるようにしてください。

## ローカル変数

可能な限りローカル変数を使用するようにしてください。
ローカル変数を使用する場合は、必ず`snake_case`で命名するようにしてください。
