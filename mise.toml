[tools]
bun = "latest"
"github:karoterra/aviutl2-aulua" = "latest"
"github:sevenc-nanashi/aviutl2-cli" = { version = "latest", bin = "au2.exe" }
"npm:rulesync" = "latest"
ruby = "latest"
stylua = "latest"

[settings]
npm = { package_manager = "bun" }

[tasks.build]
run = ["aulua build"]

[tasks.dev]
run = ["mise watch -w aulua.yaml -w Rakefile -w lua -e yaml,lua,hlsl build"]
