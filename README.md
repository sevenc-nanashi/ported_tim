# ported_tim

[ティム氏のスクリプト](https://tim3.web.fc2.com/sidx.htm)をAviUtl2用に移植するプロジェクトです。

## 差異の一覧

- アンチエイリアス：削除されました。（おそらくAviUtl2では削除されている）
- チャンネルミキサー：3つのエフェクトを重ね掛けする形式の指定は削除されました。
- チャンネルシフト：ヘルプ表示は削除されました。
- 領域枠（サイズ補助）、砕けたガラス（拡張）：それぞれの効果先のエフェクトと統合されました。
- グループ補助：削除されました。（基本効果で代用してください）

## 方針

- トラックバーなどの定義は基本的にはauluaの記法で行う。
  - トラックバーは読みやすい名前に再定義する（`rename_me_track0`などの名前はplaceholder）。
- パラメーターはAviUtl2に追加されたものを使用する。
  - 選択項目（例えば「グレー処理」など）は`select`を使用する。
    - これによって不要となったヘルプ表示のコードは削除する。
  - ダイアログの項目（`value`）は可能な限りトラックバーに置き換える。
    - 例えば「エッジしきい値」などは、トラックバーで数値を指定できるようにする。
  - `check` + `color`（「色付け」+「着色」）はnillableな`color`に置き換える。
  - `group`で項目をグループ化する。
- dllはmod2で再実装する（Rust）。
- 可能ならシェーダーを用いて処理を行う。
  - 例えば「グレー処理」などは、シェーダーでグレースケール変換を行うようにする。

## 進捗

[TASKLIST.md](./TASKLIST.md)に進捗を記録しています。

## ライセンス

現在はMIT Licenseで公開していますが、ティム氏のスクリプトのライセンスが不明なため、今後変更する可能性があります。

## 謝辞

このプロジェクトは、ティム氏のスクリプトをAviUtl2で使用できるようにすることを目的としています。
元となったスクリプトの作者であるティム氏に感謝します。
